export enum AppMode {
  APPLICATION = 'APPLICATION',
  COMPONENT = 'COMPONENT',
}

export enum AppStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum AppFrame {
  REACT = 'REACT',
  VUE = 'VUE',
}

export enum FeatureStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  ROLLOUT = 'ROLLOUT',
}
