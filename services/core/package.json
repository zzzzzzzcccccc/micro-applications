{
  "name": "@service/core",
  "version": "0.0.1",
  "description": "Service for core",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "yarn build --watch",
    "build": "rimraf dist && tsc"
  },
  "devDependencies": {
    "@nestjs/common": "^10.0.0",
    "@service/prisma": "^0.0.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.0",
    "ioredis": "^5.3.2",
    "rxjs": "^7.8.1"
  }
}
